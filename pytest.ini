[pytest]
testpaths = tests/backend
addopts = -m "not nightly"
markers =
    nightly: nightly-only catalog and planner audits, including the sampled sweep and data-sensitive baselines (deselected by default)
    migration: archived migration script tests (run with -m migration)
