---
phase: 01-landing-page-redesign
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - frontend/src/hooks/useReducedEffects.ts
  - frontend/src/components/landing/LandingHeroSimple.tsx
  - frontend/src/components/landing/BenefitsSection.tsx
  - frontend/src/components/landing/ProofSection.tsx
  - frontend/src/app/globals.css
  - frontend/tests/landingPage.dom.test.tsx
  - frontend/tests/effectsMode.test.ts
autonomous: true
requirements:
  - PERF-01
  - PERF-02
  - PERF-03
user_setup: []
must_haves:
  truths:
    - "Reduced-effects and reduced-motion users get a simpler landing experience without losing the CTA or proof ordering."
    - "Mobile visitors keep stable mockup and CTA sizing without overlap or broken sticky behavior."
    - "The final landing page passes lint, targeted DOM tests, and a production build."
  artifacts:
    - path: "frontend/src/hooks/useReducedEffects.ts"
      provides: "Shared landing-safe reduced-effects signal"
      exports:
        - "useReducedEffects"
    - path: "frontend/tests/landingPage.dom.test.tsx"
      provides: "Homepage structure and reduced-effects DOM coverage"
      min_lines: 40
    - path: "frontend/src/app/globals.css"
      provides: "Reduced-effects and mobile clamps for landing utilities"
      contains: "html[data-effects-mode=\"reduced\"]"
  key_links:
    - from: "frontend/src/components/landing/LandingHeroSimple.tsx"
      to: "frontend/src/hooks/useReducedEffects.ts"
      via: "landing reduced-effects helper"
      pattern: "useReducedEffects"
    - from: "frontend/src/components/landing/BenefitsSection.tsx"
      to: "frontend/tests/landingPage.dom.test.tsx"
      via: "test ids and reduced-mode assertions"
      pattern: "feature-spotlight"
---

<objective>
Harden the redesigned landing page for reduced-effects users, mobile, and final verification.

Purpose: Lock the visual redesign inside MarqBot's performance philosophy so the page still works when motion is reduced, screen space is tighter, and CI-style checks run.
Output: A shared reduced-effects helper, landing fallbacks, homepage DOM tests, and clean lint/test/build verification.
</objective>

<execution_context>
@C:/Users/marki/Downloads/coolsies/marqbot/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/marki/Downloads/coolsies/marqbot/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-landing-page-redesign/01-RESEARCH.md
@frontend/src/components/shared/Modal.tsx
@frontend/src/components/landing/LandingHeroSimple.tsx
@frontend/src/components/landing/BenefitsSection.tsx
@frontend/src/components/landing/ProofSection.tsx
@frontend/src/app/globals.css
@frontend/tests/effectsMode.test.ts
@frontend/tests/setupTests.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a shared reduced-effects helper and wire the landing sections to it</name>
  <files>frontend/src/hooks/useReducedEffects.ts, frontend/src/components/landing/LandingHeroSimple.tsx, frontend/src/components/landing/BenefitsSection.tsx, frontend/src/components/landing/ProofSection.tsx</files>
  <read_first>frontend/src/components/shared/Modal.tsx, frontend/src/components/landing/LandingHeroSimple.tsx, frontend/src/components/landing/BenefitsSection.tsx, frontend/src/components/landing/ProofSection.tsx, frontend/tests/effectsMode.test.ts</read_first>
  <action>Create `frontend/src/hooks/useReducedEffects.ts` exporting `useReducedEffects`. The hook must combine `useReducedMotion()` from `motion/react`, `document.documentElement.dataset.effectsMode === "reduced"`, and `window.localStorage.getItem("marqbot_effects_preference") === "reduced"` into one boolean. Use that hook inside `LandingHeroSimple.tsx`, `BenefitsSection.tsx`, and `ProofSection.tsx` so reduced mode removes float or rotate offsets, suppresses decorative ambient chips, and collapses the spotlight stage into a simple stacked layout instead of sticky storytelling. Add stable attributes for tests: `data-testid="landing-hero"` and `data-testid="feature-spotlight"` on the section roots, plus `data-reduced-motion={reduceEffects ? "true" : "false"}` on every landing section that changes behavior.</action>
  <verify>rg -n 'useReducedEffects|data-testid=\"landing-hero\"|data-testid=\"feature-spotlight\"|data-reduced-motion=' frontend/src/hooks/useReducedEffects.ts frontend/src/components/landing/LandingHeroSimple.tsx frontend/src/components/landing/BenefitsSection.tsx frontend/src/components/landing/ProofSection.tsx</verify>
  <acceptance_criteria>
    - `frontend/src/hooks/useReducedEffects.ts` exists
    - `frontend/src/hooks/useReducedEffects.ts` contains `export function useReducedEffects`
    - `frontend/src/components/landing/LandingHeroSimple.tsx` contains `data-testid="landing-hero"`
    - `frontend/src/components/landing/BenefitsSection.tsx` contains `data-testid="feature-spotlight"`
    - `frontend/src/components/landing/LandingHeroSimple.tsx` contains `data-reduced-motion=`
  </acceptance_criteria>
  <done>The landing components share one reduced-effects signal and expose stable, testable simplified-render hooks.</done>
</task>

<task type="auto">
  <name>Task 2: Add CSS fallbacks and explicit mobile clamps for the landing shell</name>
  <files>frontend/src/app/globals.css, frontend/src/components/landing/LandingHeroSimple.tsx, frontend/src/components/landing/BenefitsSection.tsx</files>
  <read_first>frontend/src/app/globals.css, frontend/src/components/landing/LandingHeroSimple.tsx, frontend/src/components/landing/BenefitsSection.tsx, .planning/REQUIREMENTS.md</read_first>
  <action>Extend `globals.css` with landing-specific fallback selectors under both `html[data-effects-mode="reduced"]` and `@media (prefers-reduced-motion: reduce)` so `.landing-hero-ambient`, `.landing-scroll-cue`, `.feature-spotlight-shell`, and any new hover sweep classes simplify or disable cleanly. Add mobile clamps for the hero mockup and CTA row using explicit `clamp()` and `max-width` values so the mockup never exceeds the viewport and CTA buttons stack below 640px. If the spotlight stage uses desktop sticky positioning, disable that sticky behavior below 1024px so mobile gets the same narrative without pinned layout tricks.</action>
  <verify>rg -n 'html\\[data-effects-mode=\"reduced\"\\]|@media \\(prefers-reduced-motion: reduce\\)|clamp\\(|max-width|1024px|640px' frontend/src/app/globals.css</verify>
  <acceptance_criteria>
    - `frontend/src/app/globals.css` contains `html[data-effects-mode="reduced"]`
    - `frontend/src/app/globals.css` contains `@media (prefers-reduced-motion: reduce)`
    - `frontend/src/app/globals.css` contains `clamp(`
    - `frontend/src/app/globals.css` contains `640px`
    - `frontend/src/app/globals.css` contains `1024px`
  </acceptance_criteria>
  <done>Reduced-effects and mobile visitors get stable landing behavior without broken sticky scenes or oversized mockups.</done>
</task>

<task type="auto">
  <name>Task 3: Add homepage DOM coverage and finish with lint, test, and build verification</name>
  <files>frontend/tests/landingPage.dom.test.tsx, frontend/tests/effectsMode.test.ts, frontend/src/components/landing/LandingHeroSimple.tsx, frontend/src/components/landing/BenefitsSection.tsx, frontend/src/components/landing/ProofSection.tsx</files>
  <read_first>frontend/tests/setupTests.ts, frontend/tests/effectsMode.test.ts, frontend/src/app/page.tsx, frontend/src/components/landing/LandingHeroSimple.tsx, frontend/src/components/landing/BenefitsSection.tsx, frontend/src/components/landing/ProofSection.tsx</read_first>
  <action>Create `frontend/tests/landingPage.dom.test.tsx` that renders `frontend/src/app/page.tsx` and asserts the landing page contains the hero CTA, the spotlight stage, the proof heading, the how-it-works section, and the final CTA in the expected order. Add a reduced-effects test that sets `document.documentElement.dataset.effectsMode = "reduced"` or `localStorage` to `marqbot_effects_preference=reduced`, renders the landing page, and asserts the hero and spotlight roots expose `data-reduced-motion="true"`. Extend `frontend/tests/effectsMode.test.ts` so the shared reduced-effects preference also covers the new landing helper. After the test files exist, run `npm --prefix frontend run lint`, `npm --prefix frontend run test -- landingPage.dom.test.tsx effectsMode.test.ts`, and `npm --prefix frontend run build`; fix only landing-related regressions until all three commands exit 0.</action>
  <verify>npm --prefix frontend run lint; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix frontend run test -- landingPage.dom.test.tsx effectsMode.test.ts; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - `frontend/tests/landingPage.dom.test.tsx` exists
    - `frontend/tests/landingPage.dom.test.tsx` contains `Get My Plan`
    - `frontend/tests/landingPage.dom.test.tsx` contains `data-reduced-motion`
    - `npm --prefix frontend run lint` exits 0
    - `npm --prefix frontend run test -- landingPage.dom.test.tsx effectsMode.test.ts` exits 0
    - `npm --prefix frontend run build` exits 0
  </acceptance_criteria>
  <done>The landing redesign is protected by homepage tests and passes lint, targeted tests, and a production build.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm --prefix frontend run lint`
- [ ] `npm --prefix frontend run test -- landingPage.dom.test.tsx effectsMode.test.ts`
- [ ] `npm --prefix frontend run build`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Reduced-effects, mobile, lint, tests, and build all validate the upgraded landing story
  </success_criteria>

<output>
After completion, create `.planning/phases/01-landing-page-redesign/01-04-SUMMARY.md`
</output>
