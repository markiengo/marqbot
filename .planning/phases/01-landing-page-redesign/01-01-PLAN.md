---
phase: 01-landing-page-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/landing/LandingHeroSimple.tsx
  - frontend/src/app/globals.css
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04
  - SYS-01
user_setup: []
must_haves:
  truths:
    - "The homepage hero owns the first desktop viewport and the product mockup is the dominant focal object on first load."
    - "The primary onboarding CTA is visible in the hero without relying on dense supporting copy."
    - "The hero exposes an obvious continuation cue toward the next story beat."
  artifacts:
    - path: "frontend/src/components/landing/LandingHeroSimple.tsx"
      provides: "Full-stage centered hero composition and hybrid mockup scene"
      min_lines: 120
      contains: "id=\"landing-hero\""
    - path: "frontend/src/app/globals.css"
      provides: "Hero-specific landing utilities and branded shell styling"
      contains: ".landing-hero-shell"
  key_links:
    - from: "frontend/src/components/landing/LandingHeroSimple.tsx"
      to: "frontend/src/app/globals.css"
      via: "landing hero utility classes"
      pattern: "landing-hero-shell|landing-hero-ambient|landing-scroll-cue"
---

<objective>
Rebuild the homepage hero as a full-stage centered product scene.

Purpose: Fix the first-viewport hierarchy problem and establish the upgraded MarqBot visual shell before any scroll choreography is layered on top.
Output: A rewritten `LandingHeroSimple` plus hero-specific landing utilities in `globals.css`.
</objective>

<execution_context>
@C:/Users/marki/Downloads/coolsies/marqbot/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/marki/Downloads/coolsies/marqbot/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-landing-page-redesign/01-RESEARCH.md
@frontend/src/app/page.tsx
@frontend/src/components/landing/LandingHeroSimple.tsx
@frontend/src/components/shared/Button.tsx
@frontend/src/components/shared/AnchorLine.tsx
@frontend/src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the hero layout around one centered stage</name>
  <files>frontend/src/components/landing/LandingHeroSimple.tsx</files>
  <read_first>frontend/src/components/landing/LandingHeroSimple.tsx, frontend/src/app/page.tsx, frontend/src/components/shared/Button.tsx, .planning/ROADMAP.md</read_first>
  <action>Rewrite `LandingHeroSimple` so the root is `<section id="landing-hero">` and the desktop layout uses `min-h-[100svh]` with the content vertically centered. Replace the current left-copy/right-mockup split with one centered stack: badge, compact headline, one-sentence support copy, CTA row, dominant mockup, and a bottom continuation cue. Keep the primary CTA text `Get My Plan`, keep the secondary CTA text `See How It Works`, and add a scroll cue link targeting `#feature-spotlight`. Keep the headline constrained to roughly 14ch-16ch on desktop so it resolves in 2-3 lines instead of spreading horizontally.</action>
  <verify>rg -n 'id=\"landing-hero\"|min-h-\\[100svh\\]|Get My Plan|See How It Works|href=\"#feature-spotlight\"' frontend/src/components/landing/LandingHeroSimple.tsx</verify>
  <acceptance_criteria>
    - `frontend/src/components/landing/LandingHeroSimple.tsx` contains `id="landing-hero"`
    - `frontend/src/components/landing/LandingHeroSimple.tsx` contains `min-h-[100svh]`
    - `frontend/src/components/landing/LandingHeroSimple.tsx` contains `href="#feature-spotlight"`
    - `frontend/src/components/landing/LandingHeroSimple.tsx` contains `Get My Plan`
    - `frontend/src/components/landing/LandingHeroSimple.tsx` contains `See How It Works`
  </acceptance_criteria>
  <done>The hero reads as one complete first-screen stage with a visible CTA row and an obvious scroll cue.</done>
</task>

<task type="auto">
  <name>Task 2: Build the centered hybrid mockup and hero shell</name>
  <files>frontend/src/components/landing/LandingHeroSimple.tsx, frontend/src/app/globals.css</files>
  <read_first>frontend/src/components/landing/LandingHeroSimple.tsx, frontend/src/app/globals.css, frontend/src/components/shared/AnchorLine.tsx, .planning/phases/01-landing-page-redesign/01-RESEARCH.md</read_first>
  <action>Replace the current preview card cluster with a centered hybrid mockup scene made of one dominant planner card, two smaller companion chips, and one supporting rail so the product is the hero focal object. Add hero utility classes in `globals.css` named `.landing-hero-shell`, `.landing-hero-ambient`, `.landing-hero-grid`, and `.landing-scroll-cue`. Keep the material treatment inside the existing MarqBot palette (`#003366`, `#0072ce`, `#ffcc00`, dark navy surfaces), use gradients, borders, and shadows instead of `backdrop-filter`, and keep any motion limited to `transform` and `opacity` only.</action>
  <verify>rg -n 'landing-hero-shell|landing-hero-ambient|landing-hero-grid|landing-scroll-cue|backdrop-filter' frontend/src/components/landing/LandingHeroSimple.tsx frontend/src/app/globals.css</verify>
  <acceptance_criteria>
    - `frontend/src/app/globals.css` contains `.landing-hero-shell`
    - `frontend/src/app/globals.css` contains `.landing-scroll-cue`
    - `frontend/src/components/landing/LandingHeroSimple.tsx` contains `landing-hero-shell`
    - `frontend/src/components/landing/LandingHeroSimple.tsx` does not contain `backdrop-filter`
    - `frontend/src/app/globals.css` does not contain new `backdrop-filter` rules for the landing hero
  </acceptance_criteria>
  <done>The hero mockup uses a branded luminous-glass shell without introducing expensive blur effects.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm --prefix frontend run lint -- src/components/landing/LandingHeroSimple.tsx`
- [ ] `rg -n 'id=\"landing-hero\"|landing-hero-shell|landing-scroll-cue' frontend/src/components/landing/LandingHeroSimple.tsx frontend/src/app/globals.css`
- [ ] `frontend/src/components/landing/LandingHeroSimple.tsx` shows the order badge -> headline -> copy -> CTA row -> mockup -> scroll cue
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The homepage hero clearly owns the opening viewport and centers the mockup as the primary first-load object
  </success_criteria>

<output>
After completion, create `.planning/phases/01-landing-page-redesign/01-01-SUMMARY.md`
</output>
