---
phase: 01-landing-page-redesign
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - frontend/src/app/page.tsx
  - frontend/src/components/landing/ProofSection.tsx
  - frontend/src/components/landing/HowItWorksClear.tsx
  - frontend/src/components/landing/LandingFinalCTA.tsx
autonomous: true
requirements:
  - FEAT-03
  - SYS-02
user_setup: []
must_haves:
  truths:
    - "Proof and trust return immediately after the spectacle beat."
    - "The lower landing sections feel like the same upgraded visual system instead of older leftovers."
    - "The final CTA remains clear and direct without competing with the hero."
  artifacts:
    - path: "frontend/src/app/page.tsx"
      provides: "Correct landing page section order including proof"
      contains: "<ProofSection />"
    - path: "frontend/src/components/landing/ProofSection.tsx"
      provides: "Rules-based proof and trust beat"
      min_lines: 90
      contains: "Why this isn&apos;t guesswork"
    - path: "frontend/src/components/landing/LandingFinalCTA.tsx"
      provides: "Upgraded close with onboarding CTA"
      contains: "/onboarding"
  key_links:
    - from: "frontend/src/app/page.tsx"
      to: "frontend/src/components/landing/ProofSection.tsx"
      via: "landing section import"
      pattern: "ProofSection"
    - from: "frontend/src/components/landing/LandingFinalCTA.tsx"
      to: "/onboarding"
      via: "primary CTA link"
      pattern: "/onboarding"
---

<objective>
Reintroduce proof/trust and retune the lower landing sections to fit the new visual system.

Purpose: Convert the post-spectacle scroll from "what is this?" into "why trust this?" and then close with lower sections that still feel connected to the upgraded hero and spotlight.
Output: Proof restored in page order, plus updated `HowItWorksClear` and `LandingFinalCTA`.
</objective>

<execution_context>
@C:/Users/marki/Downloads/coolsies/marqbot/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/marki/Downloads/coolsies/marqbot/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-landing-page-redesign/01-RESEARCH.md
@frontend/src/app/page.tsx
@frontend/src/components/landing/ProofSection.tsx
@frontend/src/components/landing/HowItWorksClear.tsx
@frontend/src/components/landing/LandingFinalCTA.tsx
@frontend/src/components/shared/AnchorLine.tsx
@frontend/src/components/shared/Button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reinsert the proof section directly after the spotlight stage</name>
  <files>frontend/src/app/page.tsx, frontend/src/components/landing/ProofSection.tsx</files>
  <read_first>frontend/src/app/page.tsx, frontend/src/components/landing/ProofSection.tsx, .planning/REQUIREMENTS.md, .planning/phases/01-landing-page-redesign/01-RESEARCH.md</read_first>
  <action>Import `ProofSection` into `frontend/src/app/page.tsx` and render it immediately after `BenefitsSection` and before `HowItWorksClear` so the landing order becomes hero -> spotlight -> proof -> how-it-works -> final CTA -> footer. In `ProofSection.tsx`, keep the existing stats and trust-card idea, but tighten the section into an early trust beat: preserve the "Why this isn't guesswork" framing, keep the rules-based credibility language, and keep advisor and student-made disclaimers visible instead of burying them in the footer.</action>
  <verify>rg -n 'ProofSection|Why this isn&apos;t guesswork|Same inputs, same plan|Built by a Marquette student' frontend/src/app/page.tsx frontend/src/components/landing/ProofSection.tsx</verify>
  <acceptance_criteria>
    - `frontend/src/app/page.tsx` contains `import { ProofSection }`
    - `frontend/src/app/page.tsx` contains `<ProofSection />`
    - `frontend/src/app/page.tsx` renders `<ProofSection />` before `<HowItWorksClear />`
    - `frontend/src/components/landing/ProofSection.tsx` contains `Why this isn&apos;t guesswork`
    - `frontend/src/components/landing/ProofSection.tsx` contains `Same inputs, same plan`
  </acceptance_criteria>
  <done>Proof/trust is restored immediately after the spectacle beat and reinforces MarqBot's rules-based credibility.</done>
</task>

<task type="auto">
  <name>Task 2: Retune how-it-works and the final CTA to match the new landing system</name>
  <files>frontend/src/components/landing/HowItWorksClear.tsx, frontend/src/components/landing/LandingFinalCTA.tsx</files>
  <read_first>frontend/src/components/landing/HowItWorksClear.tsx, frontend/src/components/landing/LandingFinalCTA.tsx, frontend/src/components/shared/AnchorLine.tsx, frontend/src/components/shared/Button.tsx</read_first>
  <action>Update `HowItWorksClear.tsx` so the three-step structure stays intact but the cards, spacing, and surfaces feel visually aligned with the upgraded hero and spotlight shell. Keep `id="how-it-works"` so the hero CTA still lands correctly, keep the "What MarqBot checks" rail, and keep motion calmer here than in section two. Update `LandingFinalCTA.tsx` to reuse the upgraded seam and material language, keep one dominant gold `Get My Plan` button linking to `/onboarding`, and make the close feel like a clean final action instead of a second hero. Do not add `backdrop-filter` or extra spectacle here.</action>
  <verify>rg -n 'id=\"how-it-works\"|What MarqBot checks|Get My Plan|/onboarding|backdrop-filter' frontend/src/components/landing/HowItWorksClear.tsx frontend/src/components/landing/LandingFinalCTA.tsx</verify>
  <acceptance_criteria>
    - `frontend/src/components/landing/HowItWorksClear.tsx` contains `id="how-it-works"`
    - `frontend/src/components/landing/HowItWorksClear.tsx` contains `What MarqBot checks`
    - `frontend/src/components/landing/LandingFinalCTA.tsx` contains `Get My Plan`
    - `frontend/src/components/landing/LandingFinalCTA.tsx` contains `/onboarding`
    - `frontend/src/components/landing/HowItWorksClear.tsx` does not contain `backdrop-filter`
  </acceptance_criteria>
  <done>The lower landing sections feel like part of the same upgraded visual system and the final CTA remains crisp and direct.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm --prefix frontend run lint -- src/app/page.tsx src/components/landing/ProofSection.tsx src/components/landing/HowItWorksClear.tsx src/components/landing/LandingFinalCTA.tsx`
- [ ] `rg -n 'ProofSection|Why this isn&apos;t guesswork|id=\"how-it-works\"|/onboarding' frontend/src/app/page.tsx frontend/src/components/landing/ProofSection.tsx frontend/src/components/landing/HowItWorksClear.tsx frontend/src/components/landing/LandingFinalCTA.tsx`
- [ ] Page order is hero -> spotlight -> proof -> how-it-works -> final CTA -> footer
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Proof/trust returns immediately after the spotlight stage and the lower sections no longer feel like visual leftovers
  </success_criteria>

<output>
After completion, create `.planning/phases/01-landing-page-redesign/01-03-SUMMARY.md`
</output>
