---
phase: 01-landing-page-redesign
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - frontend/src/components/landing/BenefitsSection.tsx
  - frontend/src/app/globals.css
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02
  - FLOW-03
  - FEAT-01
  - FEAT-02
user_setup: []
must_haves:
  truths:
    - "The first scroll feels like a designed handoff into section two instead of a generic section swap."
    - "Section two spotlights one dominant benefit at a time while supporting chips and cards reinforce the active story."
    - "Dramatic motion stays concentrated in the hero-to-spotlight transition, not every lower section."
  artifacts:
    - path: "frontend/src/components/landing/BenefitsSection.tsx"
      provides: "Spotlight-style section-two storytelling and handoff logic"
      min_lines: 140
      contains: "id=\"feature-spotlight\""
    - path: "frontend/src/app/globals.css"
      provides: "Section-two seam and spotlight utility classes"
      contains: ".feature-spotlight-shell"
  key_links:
    - from: "frontend/src/components/landing/LandingHeroSimple.tsx"
      to: "frontend/src/components/landing/BenefitsSection.tsx"
      via: "scroll cue anchor"
      pattern: "feature-spotlight"
    - from: "frontend/src/components/landing/BenefitsSection.tsx"
      to: "frontend/src/app/globals.css"
      via: "spotlight utility classes"
      pattern: "feature-spotlight-shell|feature-spotlight-rail|feature-spotlight-step"
---

<objective>
Turn section two into the single signature handoff and spotlight feature stage.

Purpose: Replace the current equal-weight benefits grid with a stronger narrative continuation that makes the first scroll memorable without creating pagewide motion noise.
Output: A rewritten `BenefitsSection` and supporting spotlight-stage CSS utilities.
</objective>

<execution_context>
@C:/Users/marki/Downloads/coolsies/marqbot/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/marki/Downloads/coolsies/marqbot/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-landing-page-redesign/01-RESEARCH.md
@frontend/src/components/landing/LandingHeroSimple.tsx
@frontend/src/components/landing/BenefitsSection.tsx
@frontend/src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite section two as a spotlight stage with one active benefit</name>
  <files>frontend/src/components/landing/BenefitsSection.tsx</files>
  <read_first>frontend/src/components/landing/BenefitsSection.tsx, frontend/src/components/landing/LandingHeroSimple.tsx, .planning/REQUIREMENTS.md, .planning/phases/01-landing-page-redesign/01-RESEARCH.md</read_first>
  <action>Replace the current three-card grid in `BenefitsSection.tsx` with `<section id="feature-spotlight">` and a two-part desktop composition: a sticky spotlight panel on the left and a scroll rail of three story steps on the right. Use `useState` plus `useInView` so each rail step sets `activeIndex` when it enters view with a margin near `-35% 0px -35% 0px`. The spotlight panel must render the currently active benefit with one dominant headline, supporting body copy, and reinforcing chips or panels. Keep mobile simple by rendering the same content as a stacked layout without sticky positioning.</action>
  <verify>rg -n 'id=\"feature-spotlight\"|useState|useInView|activeIndex|data-benefit=' frontend/src/components/landing/BenefitsSection.tsx</verify>
  <acceptance_criteria>
    - `frontend/src/components/landing/BenefitsSection.tsx` contains `id="feature-spotlight"`
    - `frontend/src/components/landing/BenefitsSection.tsx` contains `activeIndex`
    - `frontend/src/components/landing/BenefitsSection.tsx` contains `useInView`
    - `frontend/src/components/landing/BenefitsSection.tsx` contains `data-benefit=`
    - `frontend/src/components/landing/BenefitsSection.tsx` contains a desktop `sticky` spotlight container
  </acceptance_criteria>
  <done>Section two presents one dominant benefit at a time and exposes a real story rail instead of three equal cards.</done>
</task>

<task type="auto">
  <name>Task 2: Add the handoff seam and spotlight utility classes</name>
  <files>frontend/src/components/landing/BenefitsSection.tsx, frontend/src/app/globals.css</files>
  <read_first>frontend/src/components/landing/BenefitsSection.tsx, frontend/src/app/globals.css, .planning/phases/01-landing-page-redesign/01-RESEARCH.md</read_first>
  <action>Add section-two utility classes in `globals.css` named `.feature-spotlight-shell`, `.feature-spotlight-rail`, `.feature-spotlight-step`, `.feature-spotlight-chip`, and `.feature-spotlight-seam`. Use those classes inside `BenefitsSection.tsx` to create a visible seam from the hero into the spotlight stage, reinforce the active benefit with chips or companion cards, and keep all transitions limited to `transform` and `opacity`. Do not add a second sticky scene, do not animate the entire page background, and do not use `backdrop-filter` or moving blur layers.</action>
  <verify>rg -n 'feature-spotlight-shell|feature-spotlight-rail|feature-spotlight-step|feature-spotlight-chip|feature-spotlight-seam|backdrop-filter' frontend/src/components/landing/BenefitsSection.tsx frontend/src/app/globals.css</verify>
  <acceptance_criteria>
    - `frontend/src/app/globals.css` contains `.feature-spotlight-shell`
    - `frontend/src/app/globals.css` contains `.feature-spotlight-seam`
    - `frontend/src/components/landing/BenefitsSection.tsx` contains `feature-spotlight-shell`
    - `frontend/src/components/landing/BenefitsSection.tsx` does not contain `backdrop-filter`
    - `frontend/src/app/globals.css` does not add landing `backdrop-filter` rules
  </acceptance_criteria>
  <done>The hero-to-section-two transition is intentional and the spotlight stage uses reusable, performance-safe styling hooks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm --prefix frontend run lint -- src/components/landing/BenefitsSection.tsx`
- [ ] `rg -n 'id=\"feature-spotlight\"|feature-spotlight-shell|feature-spotlight-rail|activeIndex' frontend/src/components/landing/BenefitsSection.tsx frontend/src/app/globals.css`
- [ ] Section two uses one sticky spotlight and one scroll rail instead of three equal cards
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The first scroll now hands off into a spotlight-style feature stage with one dominant benefit at a time
  </success_criteria>

<output>
After completion, create `.planning/phases/01-landing-page-redesign/01-02-SUMMARY.md`
</output>
