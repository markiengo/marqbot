name: Nightly Focused Sweep

on:
  workflow_dispatch:
  schedule:
    # GitHub Actions cron is UTC. These daily entries cover 4:00 AM
    # America/Chicago across DST shifts. No hour guard — GH Actions
    # delays scheduled jobs by 30-120 min, making exact-hour checks unreliable.
    - cron: "0 9 * * *"
    - cron: "0 10 * * *"

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

concurrency:
  group: nightly-sweep
  cancel-in-progress: false

jobs:
  nightly-sweep:
    name: Nightly Focused Sweep
    runs-on: ubuntu-latest
    timeout-minutes: 240

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      - name: Resolve local schedule and seed
        id: meta
        shell: bash
        run: |
          local_date="$(TZ=America/Chicago date +%F)"
          local_time="$(TZ=America/Chicago date +%H:%M)"
          local_hour="$(TZ=America/Chicago date +%H)"
          nightly_seed="$(TZ=America/Chicago date +%Y%m%d%H%M%S)"

          echo "local_date=$local_date" >> "$GITHUB_OUTPUT"
          echo "local_time=$local_time" >> "$GITHUB_OUTPUT"
          echo "seed=$nightly_seed" >> "$GITHUB_OUTPUT"

          if [ "${{ github.event_name }}" != "schedule" ]; then
            echo "should_run=true" >> "$GITHUB_OUTPUT"
            echo "skip_reason=manual dispatch" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "should_run=true" >> "$GITHUB_OUTPUT"
          echo "skip_reason=daily schedule matched" >> "$GITHUB_OUTPUT"

      - name: Show schedule decision
        shell: bash
        run: |
          echo "event=${{ github.event_name }}"
          echo "local_date=${{ steps.meta.outputs.local_date }}"
          echo "should_run=${{ steps.meta.outputs.should_run }}"
          echo "reason=${{ steps.meta.outputs.skip_reason }}"

      - name: Install backend dependencies
        if: steps.meta.outputs.should_run == 'true'
        run: python -m pip install -r requirements.txt

      - name: Run nightly-marked planner suite
        if: steps.meta.outputs.should_run == 'true'
        id: nightly
        shell: bash
        env:
          NIGHTLY_SEED: ${{ steps.meta.outputs.seed }}
          NIGHTLY_REPORT_DATE: ${{ steps.meta.outputs.local_date }}
        run: |
          set +e
          python -m pytest -m nightly -q 2>&1 | tee nightly_output.txt
          exit_code=${PIPESTATUS[0]}
          echo "exit_code=$exit_code" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Build nightly report artifact
        if: steps.meta.outputs.should_run == 'true'
        shell: bash
        run: |
          mkdir -p nightly-artifacts
          report_path="nightly-artifacts/${{ steps.meta.outputs.local_date }}.md"
          detailed_report="tests/nightly_reports/${{ steps.meta.outputs.local_date }}.md"
          detailed_json="tests/nightly_reports/${{ steps.meta.outputs.local_date }}.json"
          title="# Nightly Planner Report"

          if grep -q "ERROR collecting" nightly_output.txt; then
            title="# Nightly Planner Report - COLLECTION ERROR"
          elif [ "${{ steps.nightly.outputs.exit_code }}" != "0" ]; then
            title="# Nightly Planner Report - FAILURES"
          fi

          {
            echo "$title"
            echo
            echo "- Local schedule: ${{ steps.meta.outputs.local_date }} America/Chicago"
            echo "- Seed: \`${{ steps.meta.outputs.seed }}\`"
            echo
            if [ "${{ steps.nightly.outputs.exit_code }}" = "0" ]; then
              echo "Pytest completed successfully."
            else
              echo "Pytest exited with code ${{ steps.nightly.outputs.exit_code }}."
            fi
            echo
            echo '```'
            tail -n 200 nightly_output.txt
            echo '```'
          } > "$report_path"

          if [ -f "$detailed_report" ]; then
            cp "$detailed_report" "nightly-artifacts/graduation-audit-${{ steps.meta.outputs.local_date }}.md"
          fi

          if [ -f "$detailed_json" ]; then
            cp "$detailed_json" "nightly-artifacts/graduation-audit-${{ steps.meta.outputs.local_date }}.json"
          fi

      - name: Publish nightly summary
        if: steps.meta.outputs.should_run == 'true'
        shell: bash
        run: |
          detailed_report="tests/nightly_reports/${{ steps.meta.outputs.local_date }}.md"
          echo "# Nightly Graduation Audit" >> "$GITHUB_STEP_SUMMARY"
          echo >> "$GITHUB_STEP_SUMMARY"
          echo "- Local date: ${{ steps.meta.outputs.local_date }} America/Chicago" >> "$GITHUB_STEP_SUMMARY"
          echo "- Seed: \`${{ steps.meta.outputs.seed }}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- Pytest exit code: ${{ steps.nightly.outputs.exit_code }}" >> "$GITHUB_STEP_SUMMARY"
          echo >> "$GITHUB_STEP_SUMMARY"

          if [ -f "$detailed_report" ]; then
            cat "$detailed_report" >> "$GITHUB_STEP_SUMMARY"
          else
            echo "Detailed graduation report was not generated. Check \`nightly_output.txt\` in the artifact." >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload nightly artifacts
        if: steps.meta.outputs.should_run == 'true'
        uses: actions/upload-artifact@v6
        with:
          name: nightly-sweep-report
          path: |
            nightly-artifacts/**
            nightly_output.txt
            tests/nightly_reports/**/*.md
            tests/nightly_reports/**/*.json
          if-no-files-found: warn
          retention-days: 14

      - name: Stop after schedule skip
        if: steps.meta.outputs.should_run != 'true'
        shell: bash
        run: |
          echo "nightly-sweep skipped: ${{ steps.meta.outputs.skip_reason }}"

      - name: Fail workflow when nightly tests fail
        if: steps.meta.outputs.should_run == 'true' && steps.nightly.outputs.exit_code != '0'
        shell: bash
        run: exit 1
