# Archived Scripts

This directory stores one-time migration and investigation scripts kept for
historical reference. They are not part of active CI/runtime workflows.

Use active scripts in `scripts/` for current workbook validation and maintenance.
